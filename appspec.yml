version: 0.0
os: linux
files:
  - source: example.tar.gz
    destination: /home/ec2-user/
    overwrite: yes

hooks:
  ApplicationStart:
    - location: scripts/start_server.sh
      timeout: 300
      runas: ec2-user
