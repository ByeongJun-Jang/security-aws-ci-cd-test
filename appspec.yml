version: 0.0
os: linux
files:
  - source: example.tar.gz
    destination: /home/ec2-user/
    overwrite: yes

hooks:
  AfterInstall:
    - location: deploy-user.sh
      timeout: 60
      runas: ec2-user
